# FFXIV 副本标题制作器｜会话交接

更新时间：2026-08-16（Asia/Shanghai）

## 给新会话的第一句话

这是一个正在迭代视觉效果的纯前端 FFXIV 风格副本标题制作器。当前用户决定先暂停视觉微调。请从现有工作区继续，先阅读本文件和代码现状；不要重置、覆盖或丢弃任何未提交改动。

## 我们在做什么

目标是制作一个可公开使用、零服务器成本的网页版标题生成器，还原《最终幻想 XIV》初入副本动画中的双行标题：上方为较小的区域名和等宽横线，下方为较大的副本名。

网站主要面向中文用户，同时必须保持独立的英文罗马衬线方案。它既能导出透明标题素材，也能让用户本地载入图片并合成完整画面。所有字体和图片只在浏览器中处理，不上传、不保存。

项目已经推送到 GitHub，并通过 Cloudflare Pages 自动部署：

- GitHub 仓库：[`SaohyiKo/FFXIV-title-maker`](https://github.com/SaohyiKo/FFXIV-title-maker)
- Git 远程地址：`git@github.com:SaohyiKo/FFXIV-title-maker.git`
- 生产网站：<https://ffxiv-title-maker.pages.dev>
- 发布分支：`main`

Cloudflare Pages 已连接该仓库。后续不需要手动上传 `dist/`：完成修改并验证后，只要将提交推送到远程仓库的 `main` 分支，Cloudflare 就会自动构建并更新上述生产网址。

## 已经完成的功能

### 页面与状态

- Vite、TypeScript、Canvas 2D 的纯前端单页工具，包管理器为 pnpm；构建输出为 `dist/`。
- 桌面端设置区和预览区双栏，移动端响应式排列。
- “自动 / 中文 / English”三种样式模式。
- 中文和英文分别保存输入草稿及参数，来回切换不会互相覆盖。
- 自动模式只根据当前文字选择样式：任一行包含汉字或 CJK 标点即采用中文方案，不主动改写文字。
- 中文默认示例为“诺弗兰特 / 末日暗影亚马乌罗提”；首次进入 English 为“NORVRANDT / AMAUROTA”。

### 字体与排版

- 默认中文字体为仓库自托管的 Source Han Sans CN Regular；页面名称显示“思源黑体”。
- 用户可临时载入自己合法持有的 OTF、TTF、WOFF、WOFF2 字体，最大 25MB。字体通过 `FontFace` 仅注册到当前浏览器内存，移除或刷新后失效。
- 项目不包含、不分发 Adobe 黑体 Std；不要把该字体加入仓库、构建产物、本地存储或上传流程。
- 英文字母始终使用 Cinzel 400；中文字符和 CJK 标点使用当前中文字体。
- Canvas 按 Unicode 字素测量和逐字绘制，横线根据区域名实际 alpha 边界确定宽度。
- 英文模式已加入小型大写式比例：每个单词首字母为 100%，其余拉丁字母为 82%。
- 当前预设：
  - 中文：32px / 72px，字距 `0.10em` / `-0.12em`，间距 4px / 8px，横线 2px，基础色 `#be944f`，外发光强度 75%。
  - 英文：38px / 92px，字距 `0.38em` / `0`，间距 5px / 10px，横线 3px，基础色 `#be944f`，外发光强度 75%。
- 中文与英文预设独立，恢复预设只恢复当前语言。

### 当前中文金属效果

- 中文使用独立于英文的金属渲染路径 `renderChineseMetalEffects()`。
- 字体蒙版经过 alpha 收紧，近似 Photoshop“犀利”抗锯齿。
- 主体始终以 `#be944f` 为基础，按每一行单独计算由上到下约 18% 的暗化渐变。
- 使用内部距离场构造约 5px 的高度场，按 120° 方位、20° 高度计算法线光照，并叠加双峰金属光泽等高线。
- 每一文字行单独计算上下位置，上半部分的高光权重大于下半部分。
- 当前相关常量和图层：
  - 高光 `#d3be97`，最后以 58% source-over 合成。
  - 浮雕阴影为纯黑，56% multiply。
  - 内发光 `#cfc7b9`，8% color-dodge。
  - 贴边描边 `#292723`，84%，轻微向下偏移。
  - 原有柔和贴边阴影 `#171614`，90%，约 5.2px 模糊，右下轻微偏移。
  - 外发光 `#7d7762`，screen，24px 范围，透明度由“外发光强度”参数换算。
- 用户最后确认的是撤销额外的独立文字投影：不要误以为还需要保留最近试过的 `CHINESE_DROP_SHADOW_COLOR` 或额外 `dropShadow` 图层；它们已经移除，当前状态就是“加额外阴影之前”。

### 英文效果

- 英文继续走 `renderLegacyEffects()`，保持原来的 Cinzel、方向性浮雕、高光、乘色阴影、内发光和外发光。
- 中文金属效果的调整不能顺手套用到英文版，除非用户明确要求。

### 背景图片与导出

- 支持 PNG、JPG、JPEG、WebP，最大 25MB；使用 `ImageBitmap`，必要时回退到 `HTMLImageElement`。
- 替换或移除图片时会释放 `ImageBitmap` 或 Object URL，避免连续上传导致内存泄漏。
- 上传后自动进入图片合成模式，也能切回透明标题模式。
- 标题水平固定居中，默认垂直中心 46%；标题大小 60%–140%，上下位置 30%–70%。
- 默认副本名字号约占图片高度 10.5%，标题完整宽度最多占图片 78%，过长时等比缩小。
- 背景不裁切、不拉伸，保持原图比例。
- 透明标题模式支持 PNG/JPG 和 1×/2×/4×。
- 图片合成模式当前只显示并导出 JPG；PNG 选项隐藏，JPG 说明小字隐藏，清晰度选择也隐藏。JPG 质量 95%。
- 单边最大 8192px、总面积约 1600 万像素；超出时按比例缩小。

### 文档

- 根目录 `README.md` 现在面向 GitHub 普通用户，并新增“当前字体设计方案”。后续修改字体、排版、配色或光效时必须同步更新该章节。
- 原来的维护说明已改名为 `README_OWNER.md`，并加入 `.gitignore`，只供项目所有者本地查看。
- 本文件 `HANDOFF.md` 用于新会话接手。

## 当前工作区状态

- Git 分支：`main`，跟踪 `origin/main`。
- Git 远程：`origin = git@github.com:SaohyiKo/FFXIV-title-maker.git`。
- 当前完整功能基线已经以提交 `aa897f1 feat: refine bilingual titles and image compositing` 推送到远程 `main`。
- GitHub 与 Cloudflare Pages 已连通，生产网址为 <https://ffxiv-title-maker.pages.dev>。
- 交接前已执行 `pnpm test`：3 个测试文件、19 项测试全部通过。
- 交接前已执行 `pnpm build`：TypeScript 检查和 Vite 生产构建通过。
- 没有正在运行的开发服务器；本地 `README_OWNER.md` 仍被 `.gitignore` 排除，不会上传到 GitHub。
- 新会话开始后应先运行 `git status --short --branch`，确认本地与 `origin/main` 的关系；如果有未提交内容，绝对不要使用 `git reset --hard`、`git checkout --`、`git clean` 或其他会丢弃工作区内容的命令。

## 当前卡在哪里

没有阻断功能开发的技术问题；用户只是决定暂时结束本次会话。

视觉上仍有一个需要长期防回归的敏感点：某些汉字的横向笔画下沿曾出现红色细线，尤其要用“当”的最后一横，以及“诺、兰、亚”等字检查。当前实现已将中文版浮雕阴影、描边和贴边阴影改成中性黑灰，避免暖红色阴影直接参与边缘混合，但尚未建立自动像素级断言，也不要宣称所有字体和浏览器组合都已彻底解决。

## 下一步计划

1. 用户下次继续时，先启动本地预览，以当前版本为基线，不主动改变效果。
2. 继续调整中文视觉前，先用固定测试文字“当 / 诺弗兰特 / 末日暗影亚马乌罗提”保存基线截图；同时检查透明网格和深色背景上的表现。
3. 如再次处理红线，只对中文版暗部颜色或单个合成层做最小改动，并确认金属高光、浮雕高度和上亮下暗渐变没有退化。
4. 为中文版关键颜色、上下亮度关系和横线下沿增加可重复的像素测试或浏览器截图回归。
5. 补足本地字体实际加载、损坏文件、连续替换背景和真实 PNG/JPG 导出的浏览器级验证。
6. 每次准备发布前，同步更新公开 README 的字体设计说明，运行测试和生产构建，并把改动整理成清晰提交。
7. 推送提交到 `origin/main`；Cloudflare Pages 会自动构建并发布，不要手动提交或上传 `dist/`。
8. 推送后打开 <https://ffxiv-title-maker.pages.dev> 验证线上版本。公开内容不得包含 Adobe 字体、用户图片或 `README_OWNER.md`。

## 踩过的坑：绝对不要再踩

1. **不要为了消除红线而对最终文字整体做去饱和、统一染色或强制覆盖。** 之前这样做虽然可能让红线消失，却同时抹掉了金属光泽和浮雕层次，用户已经明确撤销。
2. **不要把中文调色同步到英文。** 英文必须保留 Cinzel 和独立经典效果；中文版才使用高度场金属方案。
3. **不要把“字的上半部分更亮”误解成“朝上的笔画更亮”。** 用户要的是每一行文字整体从上亮到下暗的纵向趋势，不是根据单个笔画朝向切换颜色。
4. **不要用高饱和暖红或棕红色做中文版浮雕暗部。** 在抗锯齿边缘和 multiply 混合下，它会在“当”等横向笔画下沿显成红线。中文版暗部优先保持中性黑灰，金色来自字面 `#be944f`。
5. **不要用粗暴阈值、腐蚀蒙版或覆盖边缘的方式修红线。** 这会直接损伤细笔画、抗锯齿和 5px 浮雕法线，表现为“红线没了但金属质感也没了”。
6. **不要再次擅自增加独立的大范围文字投影。** 最近试过一层暖黑、右下偏移的额外投影，用户认为太硬；加大高斯模糊后仍要求整体回退。当前只保留原有贴边阴影。
7. **不要把外发光强度当作文字阴影。** 两者是不同图层，用户已经特别指出这一点。若以后需要阴影，必须先明确颜色、偏移、模糊和作用语言，并用单独图层实现。
8. **不要让基础金色漂移。** 用户多次强调主要颜色必须是 `#be944f`；高光只能提供局部材质变化，不能让整体变成高饱和亮黄。
9. **不要丢失当前脏工作区。** 现有未提交内容包含语言草稿、背景合成、英文小型大写和中文版金属渲染等完整功能。
10. **不要提交 Adobe 黑体或用户素材。** 公开版本只能自带有再分发许可的思源黑体和 Cinzel；Adobe 字体必须由用户本地临时载入。
11. **不要忘记更新公开 README 的字体设计章节。** 预设、字号、字距、颜色、混合方式、英文 82% 比例或中文版金属层只要改变，文档必须随代码同步。

## 关键文件索引

- `src/config.ts`：中英文默认文案、预设和独立草稿初始化。
- `src/types.ts`：配置、背景图、合成和导出相关类型。
- `src/utils.ts`：语言判断、Unicode 字素、英文小型大写比例、输入校验和安全画布尺寸。
- `src/font.ts`：本地中文字体加载、移除和字体就绪等待。
- `src/background.ts`：背景图片解码与资源释放。
- `src/render.ts`：文字测量、蒙版、中文/英文光效、裁切、背景合成和图片编码。
- `src/main.ts`：页面 UI、语言切换、预览、上传和导出交互。
- `src/styles.css`：布局、响应式样式及思源黑体 `@font-face`。
- `src/config.test.ts`、`src/utils.test.ts`、`src/render.test.ts`：当前 19 项单元测试。
- `README.md`：公开 GitHub 用户文档。
- `README_OWNER.md`：被忽略的本地维护说明。
